"use client";

export default function PanchangPage() {
	return (
		<section className="container mx-auto px-4 py-10 text-white">
			<div className="max-w-3xl rounded-3xl border border-white/10 bg-slate-950/80 p-6 md:p-8">
				<p className="text-xs font-semibold uppercase tracking-[0.35em] text-amber-300/80">Panchang</p>
				<h1 className="mt-3 text-4xl font-black">Panchang</h1>
				<p className="mt-4 text-slate-400">Panchang route restored as a valid module. Add the live calendar data UI here when ready.</p>
			</div>
		</section>
	);
}
